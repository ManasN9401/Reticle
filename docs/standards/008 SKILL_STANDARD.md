# Skill standard

Name the instruction scope, prerequisites and actually implemented integration. Separate dependency metadata from loaded instructions. Do not promise automatic hooks or security guarantees that the runtime does not enforce.
