diff --git a/runtime/telemetry/ui/index.html b/runtime/telemetry/ui/index.html
index 2db990a..6e8f66d 100644
--- a/runtime/telemetry/ui/index.html
+++ b/runtime/telemetry/ui/index.html
@@ -427,6 +427,20 @@
             0% { transform: scale(0.8); opacity: 0; }
             100% { transform: scale(1); opacity: 1; }
         }
+        
+        @keyframes spin {
+            100% { transform: rotate(360deg); }
+        }
+
+        .node-card.selected::after {
+            content: '';
+            position: absolute;
+            top: -50%; left: -50%; width: 200%; height: 200%;
+            background: conic-gradient(from 0deg, transparent 0%, transparent 70%, rgba(88, 166, 255, 0.8) 100%);
+            animation: spin 2s linear infinite;
+            z-index: -1;
+            border-radius: 12px;
+        }
 
         ::-webkit-scrollbar { width: 8px; }
         ::-webkit-scrollbar-track { background: transparent; }
@@ -620,7 +634,7 @@
         let activePulses = [];
         let execEdges = new Map();
         let pulseCompleted = false;
-        let globalConnectorAlpha = 0;
+        let execAlpha = new Map();
         let lastCompletedNode = null; // Stores globalId
         let timeElapsed = 0;
         let lastFrameTime = performance.now();
@@ -910,6 +924,21 @@
                 ctx.shadowColor = activeColor;
                 ctx.fill();
                 
+                // Draw selection halo
+                if (selectedNodeId === this.globalId) {
+                    ctx.save();
+                    ctx.beginPath();
+                    ctx.arc(this.x, this.y, this.radius + 12 + (this.pulse * 4), 0, Math.PI * 2);
+                    ctx.setLineDash([8, 8]);
+                    ctx.lineDashOffset = -performance.now() / 20;
+                    ctx.strokeStyle = '#a371f7';
+                    ctx.lineWidth = 2;
+                    ctx.shadowBlur = 15;
+                    ctx.shadowColor = '#a371f7';
+                    ctx.stroke();
+                    ctx.restore();
+                }
+                
                 // Draw labels
                 ctx.shadowBlur = 0;
                 ctx.fillStyle = forceColor ? forceColor : '#ffffff';
@@ -1032,8 +1061,9 @@
                         ctx.stroke();
                         
                         // Fade to green on completion
-                        if (globalConnectorAlpha > 0) {
-                            ctx.strokeStyle = `rgba(46, 160, 67, ${globalConnectorAlpha * 0.8})`;
+                        const alphaObj = execAlpha.get(parent.execId);
+                        if (alphaObj && alphaObj.current > 0) {
+                            ctx.strokeStyle = `rgba(46, 160, 67, ${alphaObj.current * 0.8})`;
                             ctx.stroke();
                         }
                     }
@@ -1124,8 +1154,11 @@
             }
             activePulses.push(...nextPulses);
             
-            if (pulseCompleted) {
-                globalConnectorAlpha = Math.min(1, globalConnectorAlpha + dt * 0.5); // Fades over 2s
+            // Update alphas
+            for (const [eId, alphaObj] of execAlpha.entries()) {
+                if (alphaObj.current < alphaObj.target) {
+                    alphaObj.current = Math.min(alphaObj.target, alphaObj.current + dt * 0.5);
+                }
             }
 
             // Draw stars normally
@@ -1278,6 +1311,7 @@
 
             // Trigger Comet Pulse on workflow complete
             if (type === 'WorkflowCompleted') {
+                execAlpha.set(execId, { target: 1, current: 0 });
                 if (lastCompletedNode && stars.has(lastCompletedNode)) {
                     const origin = stars.get(lastCompletedNode);
                     let hasParents = false;
